@inject NavigationManager NavManager
@page "/"

<style>
    /* 1. 애니메이션 정의: 둥둥 떠다니는 무빙 */
    @@keyframes floating-wobble {
        0% {
            transform: scale(1.05) rotate(-3deg) translateX(-5px);
        }

        50% {
            transform: scale(1.05) rotate(3deg) translateX(5px);
        }

        100% {
            transform: scale(1.05) rotate(-3deg) translateX(-5px);
        }
    }

    /* 2. 평소 상태 (부드러운 전환을 위해 transition 유지) */
    .github-button {
        transition: all 0.3s ease-in-out;
    }

        /* 3. 마우스 올렸을 때 효과 */
        .github-button:hover {
            /* 색상과 그림자는 즉시 적용 */
            box-shadow: 0 0 50px rgba(57, 255, 20, 0.5);
            background-color: rgba(57, 255, 20, 0.25);
            /* 아까 정의한 'floating-wobble' 애니메이션 실행 */
            /* 3s: 3초 동안 천천히 (숫자가 클수록 더 느려짐) */
            /* infinite: 마우스 올리고 있는 동안 무한 반복 */
            /* alternate: 왔다 갔다 자연스럽게 연결 */
            animation: floating-wobble 3s ease-in-out infinite alternate;
        }
</style>

<PageTitle>ReltyWorks - Home</PageTitle>

<MudStack AlignItems="AlignItems.Center" Justify="Justify.Center" Spacing="2" Class="my-16">
    <MudText Typo="Typo.h2" Color="Color.Primary" Style="font-weight:bold; text-shadow: 0 0 20px #39ff14;">
        ReltyWorks
    </MudText>
    <MudText Typo="Typo.h5" Color="Color.Primary" Class="mb-4">
        UNITY & C# DEVELOPER
    </MudText>
    <MudText Typo="Typo.body1" Align="Align.Center">
        안녕하세요! 저는 Unity와 C# 개발자입니다.<br />
        게임 개발과 프로그래밍에 열정을 가지고 있으며,<br />
        끊임없이 배우고 성장하는 개발자가 되고자 합니다.
    </MudText>
</MudStack>

<MudDivider Class="my-12" />

<MudText Typo="Typo.h4" Color="Color.Primary" Align="Align.Center" Class="mb-8">
    [ KEY PORTFOLIO ]
</MudText>

<MudCard Outlined="true" Class="pa-4" Style="border: 2px solid #39ff14; background-color: rgba(57, 255, 20, 0.05);">
    <MudCardHeader>
        <CardHeaderContent>
            <MudText Typo="Typo.h4" Color="Color.Primary" Align="Align.Center">⭐ 메인 프로젝트 제목 ⭐</MudText>
        </CardHeaderContent>
    </MudCardHeader>

    <MudCardContent>
        <MudStack AlignItems="AlignItems.Center" Class="mb-6">
            <MudAvatar Size="Size.Large" Color="Color.Primary" Variant="Variant.Outlined" Style="width:100px; height:100px; font-size:3rem;">
                🎮
            </MudAvatar>
        </MudStack>

        <MudGrid>
            <MudItem xs="12" md="6">
                <MudList T="string" Dense="true">
                    <MudListItem Icon="@Icons.Material.Filled.CalendarToday" IconColor="Color.Primary">
                        <strong>개발 기간:</strong> 2024.XX ~ 2024.XX
                    </MudListItem>
                    <MudListItem Icon="@Icons.Material.Filled.Computer" IconColor="Color.Primary">
                        <strong>개발 도구:</strong> Unity 2022, C#, VS
                    </MudListItem>
                </MudList>
            </MudItem>
            <MudItem xs="12" md="6">
                <MudList T="string" Dense="true">
                    <MudListItem Icon="@Icons.Material.Filled.Code" IconColor="Color.Primary">
                        <strong>사용 기술:</strong> Unity, C#, Design Patterns
                    </MudListItem>
                    <MudListItem Icon="@Icons.Material.Filled.Person" IconColor="Color.Primary">
                        <strong>팀 구성:</strong> 1인 개발
                    </MudListItem>
                </MudList>
            </MudItem>
        </MudGrid>

        <MudText Typo="Typo.h6" Color="Color.Primary" Class="mt-6 mb-2">📋 프로젝트 개요</MudText>
        <MudText Typo="Typo.body1">
            이 프로젝트는 [프로젝트 설명]... 여기에 가장 자랑하고 싶은 메인 프로젝트의 상세한 설명을 작성합니다.
        </MudText>

        <MudText Typo="Typo.h6" Color="Color.Primary" Class="mt-6 mb-2">💡 배운 점 & 성장</MudText>
        <MudText Typo="Typo.body1">
            이 프로젝트를 통해 배운 점들을 작성합니다.
        </MudText>
    </MudCardContent>

    <MudCardActions Class="d-flex justify-center my-4">
        <MudButton Variant="Variant.Outlined" Color="Color.Primary" Size="Size.Large" Href="/portfolios">
            &gt; 다른 프로젝트 보기
        </MudButton>
    </MudCardActions>
</MudCard>

<MudDivider Class="my-12" />

<MudGrid Class="mb-16">
    <MudItem xs="12" md="6">
        <MudText Typo="Typo.h4" Color="Color.Primary" Align="Align.Center" Class="mb-4">[ GITHUB ]</MudText>

        <MudPaper Class="d-flex flex-column align-center justify-center py-8 github-button"
                  Outlined="true"
                  Style="border-color:#39ff14; cursor:pointer;"
                  @onclick="@(() => NavigateToGithub())">

            <MudIcon Icon="@Icons.Custom.Brands.GitHub" Size="Size.Large" Color="Color.Primary" Style="font-size: 5rem;" />
            <MudText Typo="Typo.h6" Color="Color.Primary" Class="mt-4">VISIT MY GITHUB</MudText>

        </MudPaper>
    </MudItem>

    <MudItem xs="12" md="6">
        <MudText Typo="Typo.h4" Color="Color.Primary" Align="Align.Center" Class="mb-4">[ CONTACT ]</MudText>
        <MudList T="string">
            <MudListItem Icon="@Icons.Material.Filled.Email" IconColor="Color.Primary">
                your.email@example.com
            </MudListItem>
            <MudListItem Icon="@Icons.Material.Filled.Phone" IconColor="Color.Primary">
                010-XXXX-XXXX
            </MudListItem>
        </MudList>
    </MudItem>
</MudGrid>

@code {
    [Inject] NavigationManager NavMag { get; set; }

    void NavigateToGithub()
    {
        // 깃허브 주소 변경 필요
        NavMag.NavigateTo("https://github.com/ReltyWorks", true);
    }
}