<nav class="pipboy-nav-main">
    <a href="/" class="pipboy-nav-item" id="nav-home">
        HOME
    </a>

    <a href="#keyportfolio" class="pipboy-nav-item" id="nav-keyportfolio">
        KEY POPOL
    </a>

    <a href="/portfolios" class="pipboy-nav-item" id="nav-portfolios">
        PORTFOLIOS
    </a>

    <a href="#github" class="pipboy-nav-item" id="nav-github">
        GITHUB
    </a>

    <a href="#contact" class="pipboy-nav-item" id="nav-contact">
        CONTACT
    </a>
</nav>

<script>
    // 스크롤 시 메뉴 활성화
    window.addEventListener('scroll', function() {
        const sections = ['home', 'keyportfolio', 'github', 'contact'];
        const scrollPos = window.scrollY + 100;

        sections.forEach(section => {
            const element = document.getElementById(section);
            const navItem = document.getElementById('nav-' + section);

            if (element && navItem) {
                const offsetTop = element.offsetTop;
                const offsetBottom = offsetTop + element.offsetHeight;

                if (scrollPos >= offsetTop && scrollPos < offsetBottom) {
                    document.querySelectorAll('.pipboy-nav-item').forEach(item => {
                        item.classList.remove('active');
                    });
                    navItem.classList.add('active');
                }
            }
        });
    });
</script>